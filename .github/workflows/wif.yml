# .github/workflows/wif.yml
name: wif
on:
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  GCP_REGION: us-central-1
  TF_WORKING_DIR: terraform
  
jobs:
  wif:
    runs-on: ubuntu-latest
    
    permissions:
      id-token: write   # required to mint the OIDC token
      contents: read

    steps:
    
        - uses: google-github-actions/auth@v2
          with:
            workload_identity_provider: projects/455259185438/locations/global/workloadIdentityPools/github-actions/providers/github
            service_account: cgep-grc-gate-sa@civic-access-392521.iam.gserviceaccount.com
                
        - run: gcloud storage ls